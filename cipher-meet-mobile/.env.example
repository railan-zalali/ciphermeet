# ─── CipherMeet Mobile ─────────────────────────────────────────────────────────
# Mobile app configuration.
# Note: Most configuration is currently handled in src/config/env.ts

# API URL (Optional if using react-native-config)
API_URL=http://10.0.2.2:3000
